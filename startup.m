% Directory where the code of SMIR is in.
% May need to explicitly specify here e.g. /home/john/smir/, for example
base = pwd();
% base = '/home/john/smir/';

addpath(fullfile(base, 'helper'));
addpath(fullfile(base, 'plot'));

clear base
